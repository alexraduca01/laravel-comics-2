<nav class="my-nav">
    <div class="container d-flex justify-content-around">
        {{-- @foreach ( $content as $item)
            <div class="d-flex align-items-center py-5 cursor-pointer">
                <div>
                    <img class="navbar-img" src="{{ $item['image'] }}" alt="{{ $item['title'] }}">
                </div>
                <span class="ms-3 text-white text-uppercase">{{ $item['title'] }}</span>
            </div>
        @endforeach --}}
    </div>
</nav>
